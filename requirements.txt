# AgentBeats controller runtime (for containerized deployment)
# This is used by the Procfile to run: agentbeats run_ctrl
# The controller manages the agent via run.sh
earthshaker>=0.1.0

# Agent dependencies (from webnav/requirements.txt)
# These are needed when the controller runs ./run.sh to start the agent
fastapi[all]>=0.104.0
playwright>=1.40.0
pydantic>=2.0.0
uvicorn[standard]>=0.24.0
httpx>=0.25.0

